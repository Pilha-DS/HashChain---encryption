for i in range(9, 25):
        for char in all_chars:
            while tables[i].keys() == "":
                
                
            tables[i][char] = "#" + char_grafado + "#"
            char_grafado = ""